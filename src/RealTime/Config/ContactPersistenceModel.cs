namespace RealTime.Config
{
    /// <summary>Explicit contact-partner turnover assumptions, independent of export settings.</summary>
    public enum ContactPersistenceModel
    {
        LegacyPerStep = 0,
        ContextWindows = 1,
    }
}
